// Contact form submission\nconst form = document.getElementById(\'contact-form\');\nform.addEventListener(\'submit\', (event) => {\n    event.preventDefault();\n\n    const name = document.getElementById(\'name\').value;\n    const email = document.getElementById(\'email\').value;\n    const message = document.getElementById(\'message\').value;\n\n    const webhookUrl = \'https://discord.com/api/webhooks/1466459133321675026/H07LFPt5qnHLIXcTs8dvhsDf0vPs0H_w840Jq9DzHKwN6D1NVtZLqN1qc-XAy6RydxDc\';\n\n    const payload = {\n        content: `**New Contact Form Submission**\\n\\n**Name:** ${name}\\n**Email:** ${email}\\n**Message:**\\n${message}`\n    };\n\n    fetch(webhookUrl, {\n        method: \'POST\',\n        headers: {\n            \'Content-Type\': \'application/json\'\n        },\n        body: JSON.stringify(payload)\n    }).then(response => {\n        if (response.ok) {\n            alert(\'Your message has been sent!\');\n            form.reset();\n        } else {\n            alert(\'There was an error sending your message. Please try again later.\');\n        }\n    }).catch(error => {\n        console.error(\'Error:\', error);\n        alert(\'There was an error sending your message. Please try again later.\');\n    });\n});\n\n// Compute Energy Meter Animation\nconst meterFill = document.querySelector(\'.meter-fill\');\nconst meterLabel = document.querySelector(\'.meter-label\');\nlet currentEnergy = 0;\nconst maxEnergy = 100;\nconst energyPerSecond = 0.5; // kWh per second for simulation\n\nfunction updateEnergyMeter() {\n    currentEnergy = (currentEnergy + energyPerSecond) % (maxEnergy + 1);\n    const percentage = (currentEnergy / maxEnergy) * 100;\n    meterFill.style.width = `${percentage}%\`;\n    meterLabel.textContent = `Energy Use: ${currentEnergy.toFixed(2)} kWh`;\n}\n\nsetInterval(updateEnergyMeter, 1000);\n
